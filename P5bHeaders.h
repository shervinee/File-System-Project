#ifndef PROTOTYPE_H
#define PROTOTYPE_H


void archive(const char path[], char *files[], char count);
void extract(const char path[]);
void printFiles(const char path[], const char prefix[]);

#endif
